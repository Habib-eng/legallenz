services:
  - type: web
    name: LegalLens-backend
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm run server"
    envVars:
      - key: PORT
        value: 3000
    runtime: node
